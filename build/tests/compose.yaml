services:
  test_app:
    build:
      context: ../../
      dockerfile: build/tests/Dockerfile
    env_file:
      - .env.test

  test_db:
    image: postgres:18-alpine
    env_file:
      - .env.test